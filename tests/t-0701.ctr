# public key (to secure APIs etc)
Server init.

# generate keys
>> keys := Vault edkeys.

# private (secret) key
>> sk := keys first.

# public key
>> pk := keys last.

# sign a message
>> sig := Vault sign: ['abc'] with: sk.

Out write: sig length, stop.