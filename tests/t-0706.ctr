# can we override Int64?
Int64 on: ['typ'] do: { <- ['i64']. }.
>> c := (Int64 from-string: ['1']) + (Int64 from-string: ['2']).
Out write: c typ, stop.
# does + still create a stringable int64?
>> x := c string.
Out write: x, stop.
